import { Betterhighlight } from './betterhighlight';

describe('Betterhighlight', () => {
  it('should create an instance', () => {
    const directive = new Betterhighlight();
    expect(directive).toBeTruthy();
  });
});
