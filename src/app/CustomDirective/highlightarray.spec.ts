import { Highlightarray } from './highlightarray';

describe('Highlightarray', () => {
  it('should create an instance', () => {
    const directive = new Highlightarray();
    expect(directive).toBeTruthy();
  });
});
